# CEDET Produção (versão limpa)

Projeto React + Vite + Supabase.  
Login, CRUD de livros, operadores e consultores.

## Rodar localmente
```
npm install
npm run dev
```

## Deploy na Vercel
- Configure `VITE_SUPABASE_URL` e `VITE_SUPABASE_ANON_KEY` em Environment Variables.
